---
- name: Add repo to ArgoCD
  ansible.builtin.command: argocd repo add {{ item.url }} --username {{ item.username }} --password {{ item.password }} --proxy {{ item.proxy | default('') }}
  loop: "{{ repo | default([]) }}"
