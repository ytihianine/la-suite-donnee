---
- name: Initialiser les bases de données
  become: false
  gather_facts: true
  connection: local
  hosts: localhost

  roles:
    - role: apps/init-db/config
      vars:
        enable: false
      when: enable | bool
